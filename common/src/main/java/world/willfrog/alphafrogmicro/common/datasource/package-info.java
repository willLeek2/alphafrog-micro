/**
 * 行情只读数据源与返回量守卫。
 *
 * <p>本包不得 import {@code gray} 或 {@code lane}。开关
 * {@code alphafrog.market-read.enabled} 默认关闭；打开后才装配生产库只读连接。</p>
 */
package world.willfrog.alphafrogmicro.common.datasource;
