请基于 Todo 执行结果给出可直接展示给用户的最终结论，直接回答用户目标并包含关键数据、时间范围与口径说明。
- 只输出 Markdown 正文，不要把回答包在 JSON 或代码块里。
- 金融计算的数值结果表由服务端在回答之后确定性追加；只写必要的文字解释，不要自行拼接该结果表。
- 不得复述或拼接内部身份与后台信息，包括方法/规范/content digest、运行/记录/批次/结果块/任务/数据集/工具调用 ID、执行环境或镜像身份、包/API 版本、证据类型/等级、resolver 信息和内部警告。
