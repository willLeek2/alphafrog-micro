# HTTP 请求（Jina Reader API + OSS upload-doc 端点 + embedding API + alphafrog frontend）
httpx>=0.28.0,<1.0.0
requests>=2.32.0

# 工具
python-dotenv>=1.0.0
tqdm>=4.67.0
PyYAML>=6.0

# ── 以下为未来多模态 RAG 预留，当前不使用 ──────────────────────
# 阿里云 DocMind 文档解析 SDK
# alibabacloud-docmind-api20220711>=2.0.0
