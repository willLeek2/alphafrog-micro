spring:
  application:
    name: admin-service
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://${AF_DB_MAIN_HOST}:${AF_DB_MAIN_PORT}/${AF_DB_MAIN_DATABASE}
    username: ${AF_DB_MAIN_USER}
    password: ${AF_DB_MAIN_PASSWORD}

mybatis:
  configuration:
    map-underscore-to-camel-case: true

agent:
  llm:
    config-file: ${AF_AGENT_LLM_CONFIG_FILE:}
  credit:
    cny-to-usd-rate: ${AF_AGENT_CREDIT_CNY_TO_USD_RATE:0.14}

alphafrog:
  config:
    nacos:
      enabled: ${AF_CONFIG_NACOS_ENABLED:false}
      server-addr: ${NACOS_ADDRESS:127.0.0.1}:8848
      namespace: ${AF_CONFIG_NACOS_NAMESPACE:}
      group: ${AF_CONFIG_NACOS_GROUP:alphafrog-config}
      subscriptions:
        - data-id: ${AF_AGENT_LLM_CONFIG_DATA_ID:agent-llm.local.json}
          group: ${AF_CONFIG_NACOS_GROUP:alphafrog-config}
          target-file: ${AF_AGENT_LLM_CONFIG_FILE:}

dubbo:
  application:
    name: admin-service
    # 当前集群沿用 Dubbo 2 风格的接口级服务发现，显式关闭 Dubbo 3 默认的应用级双注册/双订阅
    register-mode: interface
  registry:
    address: nacos://${NACOS_ADDRESS:127.0.0.1}:8848
    # 当前未使用 Nacos 作为 Dubbo 配置中心，显式关闭默认兜底，避免启动时额外做 config-center 健康检查
    use-as-config-center: false
    # 当前未使用 Nacos 作为 Dubbo 元数据中心，显式关闭默认兜底，避免启动时额外做 metadata-center 健康检查
    use-as-metadata-center: false
  protocol:
    port: 50057
    name: tri
  consumer:
    check: false
