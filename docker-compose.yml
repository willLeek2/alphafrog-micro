# Agent LLM（OpenRouter 等）经宿主机 HTTP 代理出网。在 .env 中显式设置 HTTP_PROXY= 与 AF_JAVA_PROXY_OPTIONS= 可关闭。
x-agent-llm-proxy-env: &agent-llm-proxy-env
  HTTP_PROXY: ${HTTP_PROXY-http://host.docker.internal:7890}
  HTTPS_PROXY: ${HTTPS_PROXY-http://host.docker.internal:7890}
  http_proxy: ${HTTP_PROXY-http://host.docker.internal:7890}
  https_proxy: ${HTTPS_PROXY-http://host.docker.internal:7890}

services:
  # postgres:  <-- 用户使用的是宿主机 PG
  #   image: postgres:16-alpine
  #   container_name: alphafrog-postgres
  #   environment:
  #     POSTGRES_DB: ${AF_DB_MAIN_DATABASE:-alphafrog}
  #     POSTGRES_USER: ${AF_DB_MAIN_USER:-alphafrog}
  #     POSTGRES_PASSWORD: ${AF_DB_MAIN_PASSWORD:-alphafrog_pass}
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - alphafrog_pgdata:/var/lib/postgresql/data
  #     - ./portfolio_schema.sql:/docker-entrypoint-initdb.d/10_portfolio_schema.sql:ro
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 20
  #   networks:
  #     - alphafrog-network

  redis:
    image: redis:7-alpine
    container_name: alphafrog-redis
    command: ["redis-server", "--appendonly", "yes", "--requirepass", "${AF_REDIS_PASSWORD:-Excited1s}", "--maxmemory", "200mb", "--maxmemory-policy", "allkeys-lru"]
    ports:
      - "6379:6379"
    volumes:
      - alphafrog_redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${AF_REDIS_PASSWORD:-Excited1s}", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20
    deploy:
      resources:
        limits:
          memory: 256M
    networks:
      - alphafrog-network

  # zookeeper/kafka: 已移除，迁移至 RabbitMQ

  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    container_name: alphafrog-rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: ${AF_RABBITMQ_USER:-alphafrog}
      RABBITMQ_DEFAULT_PASS: ${AF_RABBITMQ_PASS:-Excited1s}
      RABBITMQ_SERVER_ADDITIONAL_ERL_ARGS: >-
        +P 1048576
        +sbwt none
        +sbwtdcpu none
        +sbwtdio none
        -rabbit vm_memory_high_watermark 0.70
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - alphafrog_rabbitmq:/var/lib/rabbitmq
    deploy:
      resources:
        limits:
          memory: 512M
    healthcheck:
      test: rabbitmq-diagnostics -q ping
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - alphafrog-network

  nacos:
    image: nacos/nacos-server:v2.5.0
    container_name: alphafrog-nacos
    environment:
      MODE: standalone
      # 生产环境下 hostname 可能是容器随机名，Dubbo/Nacos 客户端未必能稳定解析，统一改为 IP 暴露
      PREFER_HOST_MODE: ip
      NACOS_AUTH_ENABLE: "false"
      # Memory Optimization
      JVM_XMS: 512m
      JVM_XMX: 512m
    ports:
      - "8848:8848"
    volumes:
      - ./data/nacos/logs:/home/nacos/logs
      - ./data/nacos/data:/home/nacos/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8848/nacos/v1/console/health/readiness"]
      interval: 10s
      timeout: 10s
      retries: 20
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 1G
    networks:
      - alphafrog-network

  meilisearch:
    image: getmeili/meilisearch:v1.12
    container_name: alphafrog-meilisearch
    environment:
      MEILI_MASTER_KEY: ${AF_MEILI_API_KEY:-alphafrog_search_key}
      MEILI_HTTP_ADDR: 0.0.0.0:7700
      MEILI_MAX_INDEXING_MEMORY: 600Mb
      MEILI_MAX_INDEXING_THREADS: 1
      MEILI_EXPERIMENTAL_REDUCE_INDEXING_MEMORY_USAGE: "true"
      MEILI_EXPERIMENTAL_SEARCH_QUEUE_SIZE: 100
    ports:
      - "7700:7700"
    volumes:
      - ./data/meilisearch:/meili_data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:7700/health"]
      interval: 10s
      timeout: 5s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 1G
    networks:
      - alphafrog-network

  domestic-stock-service:
    image: alphafrog-micro-domestic-stock-service:latest
    build: ./domesticStockService
    container_name: alphafrog-domestic-stock-service
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      redis:
        condition: service_healthy
      nacos:
        condition: service_healthy
      meilisearch:
        condition: service_healthy
    environment:
      TZ: Asia/Shanghai
      AF_DB_MAIN_HOST: ${AF_DB_MAIN_HOST:-host.docker.internal}
      AF_DB_MAIN_PORT: 5432
      AF_DB_MAIN_DATABASE: ${AF_DB_MAIN_DATABASE:-alphafrog}
      AF_DB_MAIN_USER: ${AF_DB_MAIN_USER:-alphafrog}
      AF_DB_MAIN_PASSWORD: ${AF_DB_MAIN_PASSWORD:-alphafrog_pass}
      AF_REDIS_HOST: redis
      AF_REDIS_PASSWORD: ${AF_REDIS_PASSWORD:-Excited1s}
      AF_MEILI_HOST: http://meilisearch:7700
      AF_MEILI_API_KEY: ${AF_MEILI_API_KEY:-alphafrog_search_key}
      NACOS_ADDRESS: nacos
      TUSHARE_TOKEN: ${TUSHARE_TOKEN}
      # Memory Optimization (Compressed)
      # domestic-stock-service 集成较重（Dubbo+MeiliSearch+Redis），Metaspace 需要更多空间
      JAVA_OPTS: >-
        -XX:+UseSerialGC
        -Xss512k
        -Xms64m
        -Xmx200m
        -XX:MaxRAM=420m
        -XX:MaxMetaspaceSize=128m
    ports:
      - "50055:50055"
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 200M
    networks:
      - alphafrog-network

  domestic-index-service:
    image: alphafrog-micro-domestic-index-service:latest
    build: ./domesticIndexService
    container_name: alphafrog-domestic-index-service
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      redis:
        condition: service_healthy
      nacos:
        condition: service_healthy
      meilisearch:
        condition: service_healthy
    environment:
      TZ: Asia/Shanghai
      AF_DB_MAIN_HOST: ${AF_DB_MAIN_HOST:-host.docker.internal}
      AF_DB_MAIN_PORT: 5432
      AF_DB_MAIN_DATABASE: ${AF_DB_MAIN_DATABASE:-alphafrog}
      AF_DB_MAIN_USER: ${AF_DB_MAIN_USER:-alphafrog}
      AF_DB_MAIN_PASSWORD: ${AF_DB_MAIN_PASSWORD:-alphafrog_pass}
      AF_REDIS_HOST: redis
      AF_REDIS_PASSWORD: ${AF_REDIS_PASSWORD:-Excited1s}
      AF_MEILI_HOST: http://meilisearch:7700
      AF_MEILI_API_KEY: ${AF_MEILI_API_KEY:-alphafrog_search_key}
      NACOS_ADDRESS: nacos
      TUSHARE_TOKEN: ${TUSHARE_TOKEN}
      # Memory Optimization (Compressed)
      JAVA_OPTS: >-
        -XX:+UseSerialGC
        -Xss512k
        -Xms64m
        -Xmx200m
        -XX:MaxRAM=420m
        -XX:MaxMetaspaceSize=128m
    ports:
      - "50053:50053"
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 200M
    networks:
      - alphafrog-network

  domestic-fund-service:
    image: alphafrog-micro-domestic-fund-service:latest
    build: ./domesticFundService
    container_name: alphafrog-domestic-fund-service
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      redis:
        condition: service_healthy
      nacos:
        condition: service_healthy
      meilisearch:
        condition: service_healthy
    environment:
      TZ: Asia/Shanghai
      AF_DB_MAIN_HOST: ${AF_DB_MAIN_HOST:-host.docker.internal}
      AF_DB_MAIN_PORT: 5432
      AF_DB_MAIN_DATABASE: ${AF_DB_MAIN_DATABASE:-alphafrog}
      AF_DB_MAIN_USER: ${AF_DB_MAIN_USER:-alphafrog}
      AF_DB_MAIN_PASSWORD: ${AF_DB_MAIN_PASSWORD:-alphafrog_pass}
      AF_REDIS_HOST: redis
      AF_REDIS_PASSWORD: ${AF_REDIS_PASSWORD:-Excited1s}
      AF_MEILI_HOST: http://meilisearch:7700
      AF_MEILI_API_KEY: ${AF_MEILI_API_KEY:-alphafrog_search_key}
      NACOS_ADDRESS: nacos
      TUSHARE_TOKEN: ${TUSHARE_TOKEN}
      # Memory Optimization (Compressed)
      JAVA_OPTS: >-
        -XX:+UseSerialGC
        -Xss512k
        -Xms64m
        -Xmx200m
        -XX:MaxRAM=420m
        -XX:MaxMetaspaceSize=128m
    ports:
      - "50052:50052"
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 200M
    networks:
      - alphafrog-network

  domestic-listed-asset-service:
    image: alphafrog-micro-domestic-listed-asset-service:latest
    build: ./domesticListedAssetService
    container_name: alphafrog-domestic-listed-asset-service
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      nacos:
        condition: service_healthy
      meilisearch:
        condition: service_healthy
    environment:
      TZ: Asia/Shanghai
      AF_DB_MAIN_HOST: ${AF_DB_MAIN_HOST:-host.docker.internal}
      AF_DB_MAIN_PORT: 5432
      AF_DB_MAIN_DATABASE: ${AF_DB_MAIN_DATABASE:-alphafrog}
      AF_DB_MAIN_USER: ${AF_DB_MAIN_USER:-alphafrog}
      AF_DB_MAIN_PASSWORD: ${AF_DB_MAIN_PASSWORD:-alphafrog_pass}
      AF_MEILI_HOST: http://meilisearch:7700
      AF_MEILI_API_KEY: ${AF_MEILI_API_KEY:-alphafrog_search_key}
      NACOS_ADDRESS: nacos
      # Memory Optimization (Compressed)
      JAVA_OPTS: >-
        -XX:+UseSerialGC
        -Xss512k
        -Xms64m
        -Xmx200m
        -XX:MaxRAM=420m
        -XX:MaxMetaspaceSize=128m
    ports:
      - "50062:50062"
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 200M
    networks:
      - alphafrog-network

  domestic-fetch-service:
    image: alphafrog-micro-domestic-fetch-service:latest
    build: ./domesticFetchService
    container_name: alphafrog-domestic-fetch-service
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      nacos:
        condition: service_healthy
    environment:
      TZ: Asia/Shanghai
      AF_DB_MAIN_HOST: ${AF_DB_MAIN_HOST:-host.docker.internal}
      AF_DB_MAIN_PORT: 5432
      AF_DB_MAIN_DATABASE: ${AF_DB_MAIN_DATABASE:-alphafrog}
      AF_DB_MAIN_USER: ${AF_DB_MAIN_USER:-alphafrog}
      AF_DB_MAIN_PASSWORD: ${AF_DB_MAIN_PASSWORD:-alphafrog_pass}
      AF_RABBITMQ_HOST: rabbitmq
      AF_RABBITMQ_PORT: 5672
      AF_RABBITMQ_USER: ${AF_RABBITMQ_USER:-alphafrog}
      AF_RABBITMQ_PASS: ${AF_RABBITMQ_PASS:-Excited1s}
      AF_REDIS_HOST: redis
      AF_REDIS_PASSWORD: ${AF_REDIS_PASSWORD:-Excited1s}
      NACOS_ADDRESS: nacos
      NACOS_USER: ${NACOS_USER:-nacos}
      NACOS_PASSWORD: ${NACOS_PASSWORD:-nacos}
      TUSHARE_TOKEN: ${TUSHARE_TOKEN}
      LOGGING_LEVEL_APP: ${LOGGING_LEVEL_APP:-INFO}
      AF_FETCH_CONCURRENCY: ${AF_FETCH_CONCURRENCY:-3}
      AF_CONFIG_NACOS_ENABLED: "true"
      AF_CONFIG_NACOS_NAMESPACE: ${AF_CONFIG_NACOS_NAMESPACE:-}
      AF_CONFIG_NACOS_GROUP: "alphafrog-config"
      AF_FETCH_JOBS_CONFIG_FILE: "/app/config-dynamic/fetch-jobs.json"
      AF_FETCH_JOBS_CONFIG_REFRESH_INTERVAL_MS: 10000
      # Memory Optimization (Compressed)
      JAVA_OPTS: >-
        -XX:+UseSerialGC
        -Xss512k
        -Xms64m
        -Xmx200m
        -XX:MaxRAM=420m
        -XX:MaxMetaspaceSize=128m
    volumes:
      - ./domesticFetchService/config:/app/config:ro
      - ./data/domestic-fetch-configs:/app/config-dynamic
    ports:
      - "20882:20882"
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 200M
    networks:
      - alphafrog-network

  admin-service:
    image: alphafrog-micro-admin-service:latest
    build: ./adminService
    container_name: alphafrog-admin-service
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      nacos:
        condition: service_healthy
    environment:
      TZ: Asia/Shanghai
      AF_DB_MAIN_HOST: ${AF_DB_MAIN_HOST:-host.docker.internal}
      AF_DB_MAIN_PORT: 5432
      AF_DB_MAIN_DATABASE: ${AF_DB_MAIN_DATABASE:-alphafrog}
      AF_DB_MAIN_USER: ${AF_DB_MAIN_USER:-alphafrog}
      AF_DB_MAIN_PASSWORD: ${AF_DB_MAIN_PASSWORD:-alphafrog_pass}
      NACOS_ADDRESS: nacos
      ADMIN_MAGIC_PASSWORD: ${ADMIN_MAGIC_PASSWORD:-frog20191127StartFromBelieving}
      # Memory Optimization (Compressed)
      JAVA_OPTS: >-
        -XX:+UseSerialGC
        -Xss512k
        -Xms64m
        -Xmx200m
        -XX:MaxRAM=420m
        -XX:MaxMetaspaceSize=128m
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 200M
    networks:
      - alphafrog-network

  portfolio-service:
    image: alphafrog-micro-portfolio-service:latest
    build: ./portfolioService
    container_name: alphafrog-portfolio-service
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      nacos:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    environment:
      TZ: Asia/Shanghai
      AF_DB_MAIN_HOST: ${AF_DB_MAIN_HOST:-host.docker.internal}
      AF_DB_MAIN_PORT: 5432
      AF_DB_MAIN_DATABASE: ${AF_DB_MAIN_DATABASE:-alphafrog}
      AF_DB_MAIN_USER: ${AF_DB_MAIN_USER:-alphafrog}
      AF_DB_MAIN_PASSWORD: ${AF_DB_MAIN_PASSWORD:-alphafrog_pass}
      AF_RABBITMQ_HOST: rabbitmq
      AF_RABBITMQ_PORT: 5672
      AF_RABBITMQ_USER: ${AF_RABBITMQ_USER:-alphafrog}
      AF_RABBITMQ_PASS: ${AF_RABBITMQ_PASS:-Excited1s}
      NACOS_ADDRESS: nacos
      NACOS_USER: ${NACOS_USER:-nacos}
      NACOS_PASSWORD: ${NACOS_PASSWORD:-nacos}
      # Memory Optimization (Compressed)
      JAVA_OPTS: >-
        -XX:+UseSerialGC
        -Xss512k
        -Xms64m
        -Xmx200m
        -XX:MaxRAM=420m
        -XX:MaxMetaspaceSize=128m
    ports:
      - "50056:50056"
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 200M
    networks:
      - alphafrog-network

  agent-langchain-service:
    image: alphafrog-micro-agent-langchain-service:latest
    build: ./agentLangchainService
    container_name: alphafrog-agent-langchain-service
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      nacos:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      TZ: Asia/Shanghai
      AF_DB_MAIN_HOST: ${AF_DB_MAIN_HOST:-host.docker.internal}
      AF_DB_MAIN_PORT: 5432
      AF_DB_MAIN_DATABASE: ${AF_DB_MAIN_DATABASE:-alphafrog}
      AF_DB_MAIN_USER: ${AF_DB_MAIN_USER:-alphafrog}
      AF_DB_MAIN_PASSWORD: ${AF_DB_MAIN_PASSWORD:-alphafrog_pass}
      AF_REDIS_HOST: redis
      AF_REDIS_PORT: 6379
      AF_REDIS_PASSWORD: ${AF_REDIS_PASSWORD:-Excited1s}
      NACOS_ADDRESS: nacos
      # LangChain agent provider（与 legacy 隔离）
      SPRING_PROFILES_ACTIVE: langchain-provider
      AGENT_LANGCHAIN_PROVIDER_ENABLED: "true"
      AGENT_LANGCHAIN_PROVIDER_DUBBO_GROUP: langchain
      AGENT_LANGCHAIN_TRAFFIC_CANARY_PERCENT: ${AGENT_LANGCHAIN_TRAFFIC_CANARY_PERCENT:-0}
      AGENT_LANGCHAIN_DUBBO_PORT: 50059
      AGENT_LANGCHAIN_SERVER_PORT: 50158
      # Agent LLM 配置（使用 agentLangchainService/config 作为共享静态 prompt/config 目录）
      AF_AGENT_LLM_CONFIG_FILE: ${AF_AGENT_LLM_CONFIG_FILE:-/app/config-dynamic/agent-llm.local.json}
      AF_AGENT_LLM_PROMPT_BASE_DIR: ${AF_AGENT_LLM_PROMPT_BASE_DIR:-/app/config}
      AF_AGENT_API_KEY: ${AF_AGENT_API_KEY:-demo}
      AF_AGENT_DEFAULT_ENDPOINT: ${AF_AGENT_DEFAULT_ENDPOINT:-openrouter}
      AF_AGENT_DEFAULT_MODEL: ${AF_AGENT_DEFAULT_MODEL:-openai/gpt-5.2}
      AF_AGENT_MODELS: ${AF_AGENT_MODELS:-openai/gpt-5.2,openai/gpt-5.1,google/gemini-3-pro-preview,moonshotai/kimi-k2.5,accounts/fireworks/models/kimi-k2p5}
      AF_AGENT_ENDPOINT_OPENROUTER: ${AF_AGENT_ENDPOINT_OPENROUTER:-https://openrouter.ai/api/v1}
      AF_AGENT_ENDPOINT_OPENAI: ${AF_AGENT_ENDPOINT_OPENAI:-https://api.openai.com/v1}
      AF_AGENT_ENDPOINT_FIREWORKS: ${AF_AGENT_ENDPOINT_FIREWORKS:-https://api.fireworks.ai/inference/v1}
      AF_AGENT_ENDPOINT_OPENROUTER_API_KEY: ${AF_AGENT_ENDPOINT_OPENROUTER_API_KEY:-}
      AF_AGENT_ENDPOINT_OPENAI_API_KEY: ${AF_AGENT_ENDPOINT_OPENAI_API_KEY:-}
      AF_AGENT_ENDPOINT_FIREWORKS_API_KEY: ${AF_AGENT_ENDPOINT_FIREWORKS_API_KEY:-}
      AF_AGENT_ENDPOINT_DASHSCOPE_API_KEY: ${AF_AGENT_ENDPOINT_DASHSCOPE_API_KEY:-}
      AF_AGENT_ENDPOINT_DASHSCOPE_SG_API_KEY: ${AF_AGENT_ENDPOINT_DASHSCOPE_SG_API_KEY:-}
      AF_AGENT_ENDPOINT_DASHSCOPE_CN_API_KEY: ${AF_AGENT_ENDPOINT_DASHSCOPE_CN_API_KEY:-}
      AF_AGENT_BASE_URL: ${AF_AGENT_BASE_URL:-https://openrouter.ai/api/v1}
      AF_AGENT_MODEL_NAME: ${AF_AGENT_MODEL_NAME:-openai/gpt-5.2}
      AF_AGENT_MAX_TOKENS: ${AF_AGENT_MAX_TOKENS:-4096}
      AF_AGENT_TEMPERATURE: ${AF_AGENT_TEMPERATURE:-0.7}
      AF_AGENT_REASONING_EFFORT: ${AF_AGENT_REASONING_EFFORT:-}
      AF_AGENT_REASONING_MAX_TOKENS: ${AF_AGENT_REASONING_MAX_TOKENS:-}
      AF_AGENT_REASONING_ENABLED: ${AF_AGENT_REASONING_ENABLED:-}
      AF_AGENT_REASONING_EXCLUDE: ${AF_AGENT_REASONING_EXCLUDE:-false}
      LOGGING_LEVEL_AGENT: ${LOGGING_LEVEL_AGENT:-INFO}
      # Market-data dataset / manifest 开关
      AF_AGENT_MARKET_DATA_DATASET_ENABLED: "true"
      AF_AGENT_MARKET_DATA_BATCH_EMIT_MANIFEST: "true"
      AF_CONFIG_NACOS_ENABLED: "true"
      AF_CONFIG_NACOS_NAMESPACE: ${AF_CONFIG_NACOS_NAMESPACE:-}
      AF_CONFIG_NACOS_DATA_ID: "code-refine.json"
      AF_CONFIG_NACOS_GROUP: "alphafrog-config"
      AF_AGENT_FLOW_CODE_REFINE_CONFIG_FILE: "/app/config-dynamic/code-refine.local.json"
      JAVA_TOOL_OPTIONS: "-Xms512m -Xmx1g ${AF_JAVA_PROXY_OPTIONS--Dhttp.proxyHost=host.docker.internal -Dhttp.proxyPort=7890 -Dhttps.proxyHost=host.docker.internal -Dhttps.proxyPort=7890 -Dhttp.nonProxyHosts=localhost|127.0.0.1|nacos|redis|rabbitmq|meilisearch|host.docker.internal}"
      <<: *agent-llm-proxy-env
      NO_PROXY: ${NO_PROXY-localhost,127.0.0.1,nacos,redis,rabbitmq,meilisearch,host.docker.internal}
      no_proxy: ${NO_PROXY-localhost,127.0.0.1,nacos,redis,rabbitmq,meilisearch,host.docker.internal}
    volumes:
      - ./data/agent_datasets:/data/agent_datasets
      - ./data/database_fetched:/data/database_fetched
      - ./data/manifests:/data/manifests
      - ./data/agent_artifacts:/data/agent_artifacts
      - ./data/agent_workspaces:/data/agent_workspaces
      - ./agentPlatformShared/src/main/resources/agent_guides:/data/agent_guides:ro
      - ./agentLangchainService/config:/app/config:ro
      - ./data/agent-configs:/app/config-dynamic
    ports:
      - "50059:50059"
      - "50158:50158"
    deploy:
      resources:
        limits:
          memory: 1.5G
    networks:
      - alphafrog-network

  external-info-service:
    image: alphafrog-micro-external-info-service:latest
    build: ./externalInfoService
    container_name: alphafrog-external-info-service
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      nacos:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      TZ: Asia/Shanghai
      NACOS_ADDRESS: nacos
      AF_CONFIG_NACOS_ENABLED: "true"
      AF_CONFIG_NACOS_NAMESPACE: ${AF_CONFIG_NACOS_NAMESPACE:-}
      AF_CONFIG_NACOS_GROUP: "alphafrog-config"
      AF_EXTERNAL_INFO_SEARCH_LLM_CONFIG_FILE: ${AF_EXTERNAL_INFO_SEARCH_LLM_CONFIG_FILE:-/app/config-dynamic/search-llm.local.json}
      AF_EXTERNAL_INFO_SEARCH_LLM_PROMPT_BASE_DIR: ${AF_EXTERNAL_INFO_SEARCH_LLM_PROMPT_BASE_DIR:-/app/config}
      AF_EXTERNAL_INFO_SEARCH_PPLX_API_KEY: ${AF_EXTERNAL_INFO_SEARCH_PPLX_API_KEY:-}
      AF_EXTERNAL_INFO_SEARCH_TAVILY_API_KEY: ${AF_EXTERNAL_INFO_SEARCH_TAVILY_API_KEY:-}
      AF_EXTERNAL_INFO_SEARCH_EXA_API_KEY: ${AF_EXTERNAL_INFO_SEARCH_EXA_API_KEY:-}
      LOGGING_LEVEL_EXTERNAL_INFO: ${LOGGING_LEVEL_EXTERNAL_INFO:-INFO}
      AF_DB_MAIN_HOST: ${AF_DB_MAIN_HOST:-host.docker.internal}
      AF_DB_MAIN_PORT: 5432
      AF_DB_MAIN_DATABASE: ${AF_DB_MAIN_DATABASE:-alphafrog}
      AF_DB_MAIN_USER: ${AF_DB_MAIN_USER:-alphafrog}
      AF_DB_MAIN_PASSWORD: ${AF_DB_MAIN_PASSWORD:-alphafrog_pass}
      # 热点 WebSearch 缓存与统计使用 Redis；默认连接本 compose 内的 redis 服务
      AF_REDIS_HOST: redis
      AF_REDIS_PORT: 6379
      AF_REDIS_PASSWORD: ${AF_REDIS_PASSWORD:-Excited1s}
      SPRING_DATA_REDIS_HOST: redis
      SPRING_DATA_REDIS_PORT: 6379
      SPRING_DATA_REDIS_PASSWORD: ${AF_REDIS_PASSWORD:-Excited1s}
      AF_QDRANT_HOST: shared-qdrant
      AF_QDRANT_PORT: 6334
      AF_QDRANT_HTTP_PORT: 6333
      AF_RAG_EMBEDDING_CONFIG_FILE: ${AF_RAG_EMBEDDING_CONFIG_FILE:-/app/config-dynamic/rag-embedding.local.json}
      AF_EMBEDDING_API_KEY: ${AF_EMBEDDING_API_KEY:-}
      # HTTP/HTTPS 代理（访问 Perplexity/Exa/Tavily 等境外服务时使用）。
      # 使用 service-specific 变量，避免部署机 shell 的 HTTP_PROXY=http://127.0.0.1:7890
      # 被直接带入容器；容器内 127.0.0.1 指向容器自身，不是宿主机代理。
      HTTP_PROXY: ${AF_EXTERNAL_INFO_HTTP_PROXY-http://host.docker.internal:7890}
      HTTPS_PROXY: ${AF_EXTERNAL_INFO_HTTPS_PROXY-http://host.docker.internal:7890}
      http_proxy: ${AF_EXTERNAL_INFO_HTTP_PROXY-http://host.docker.internal:7890}
      https_proxy: ${AF_EXTERNAL_INFO_HTTPS_PROXY-http://host.docker.internal:7890}
      NO_PROXY: ${AF_EXTERNAL_INFO_NO_PROXY-localhost,127.0.0.1,::1,nacos,redis,rabbitmq,meilisearch,shared-qdrant,host.docker.internal}
      no_proxy: ${AF_EXTERNAL_INFO_NO_PROXY-localhost,127.0.0.1,::1,nacos,redis,rabbitmq,meilisearch,shared-qdrant,host.docker.internal}
      AF_RAG_INGEST_TOKEN: ${AF_RAG_INGEST_TOKEN:-}
      AF_OSS_ENDPOINT: ${AF_OSS_ENDPOINT:-oss-ap-southeast-1-internal.aliyuncs.com}
      AF_OSS_BUCKET: ${AF_OSS_BUCKET}
      AF_OSS_ACCESS_KEY_ID: ${AF_OSS_ACCESS_KEY_ID}
      AF_OSS_ACCESS_KEY_SECRET: ${AF_OSS_ACCESS_KEY_SECRET}
      AF_OSS_PATH_PREFIX: ${AF_OSS_PATH_PREFIX:-alphafrog-rag}
      JAVA_TOOL_OPTIONS: "-Xms256m -Xmx512m ${AF_EXTERNAL_INFO_JAVA_PROXY_OPTIONS--Dhttp.proxyHost=host.docker.internal -Dhttp.proxyPort=7890 -Dhttps.proxyHost=host.docker.internal -Dhttps.proxyPort=7890 -Dhttp.nonProxyHosts=localhost|127.0.0.1|nacos|redis|rabbitmq|meilisearch|shared-qdrant|host.docker.internal}"
      DUBBO_PROTOCOL_HOST: ${DUBBO_PROTOCOL_HOST:-}
    volumes:
      - ./externalInfoService/config:/app/config:ro
      - ./data/external-info-configs:/app/config-dynamic
    ports:
      - "50061:50061"
    deploy:
      resources:
        limits:
          memory: 768M
    # external-info-service 连接两个网络：
    #   eth0 = shared-infra (外部网络，Docker 会先连接 external 网络)
    #   eth1 = alphafrog-network (项目内部网络，Dubbo 应该注册到这个网络的 IP)
    # IP 获取逻辑已移至 Dockerfile 的 entrypoint.sh
    networks:
      - alphafrog-network
      - shared-infra          # eth1，仅用于 DNS 解析 shared-qdrant 容器名

  python-sandbox-service:
    image: alphafrog-python-sandbox:latest
    build: ./pythonSandboxService
    container_name: alphafrog-python-sandbox-service
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      AF_SANDBOX_DATA_DIR: /data/database_fetched
      AF_SANDBOX_DEBUG: ${AF_SANDBOX_DEBUG:-false}
      AF_SANDBOX_MAX_CONCURRENCY: ${AF_SANDBOX_MAX_CONCURRENCY:-20}
      AF_SANDBOX_EXECUTION_TIMEOUT: ${AF_SANDBOX_EXECUTION_TIMEOUT:-5}
      AF_SANDBOX_MEMORY: ${AF_SANDBOX_MEMORY:-512m}
      AF_SANDBOX_MEMSWAP: ${AF_SANDBOX_MEMSWAP:-512m}
      AF_SANDBOX_BACKEND: ${AF_SANDBOX_BACKEND:-docker}
      AF_SANDBOX_WORKDIR: /sandbox
      AF_SANDBOX_LOG_LEVEL: ${AF_SANDBOX_LOG_LEVEL:-INFO}
      AF_SANDBOX_IMAGE: ${AF_SANDBOX_IMAGE:-alphafrog-sandbox-runtime:latest}
      AF_SANDBOX_SKIP_ENVIRONMENT_SETUP: ${AF_SANDBOX_SKIP_ENVIRONMENT_SETUP:-true}
      AF_SANDBOX_PREINSTALLED_LIBRARIES: ${AF_SANDBOX_PREINSTALLED_LIBRARIES:-numpy,pandas,matplotlib,scipy}
      # Compose/default deployment exercises the pooled runner. Set
      # AF_SANDBOX_POOL_ENABLED=false in the host env/.env to force the legacy per-task SandboxSession path.
      AF_SANDBOX_POOL_ENABLED: ${AF_SANDBOX_POOL_ENABLED:-true}
      AF_SANDBOX_POOL_MIN_SIZE: ${AF_SANDBOX_POOL_MIN_SIZE:-3}
      AF_SANDBOX_POOL_MAX_SIZE: ${AF_SANDBOX_POOL_MAX_SIZE:-8}
      AF_SANDBOX_POOL_ACQUIRE_TIMEOUT: ${AF_SANDBOX_POOL_ACQUIRE_TIMEOUT:-30}
      AF_SANDBOX_POOL_IDLE_TIMEOUT: ${AF_SANDBOX_POOL_IDLE_TIMEOUT:-300}
      AF_SANDBOX_POOL_MAX_CONTAINER_USES: ${AF_SANDBOX_POOL_MAX_CONTAINER_USES:-}
      AF_SANDBOX_WORKSPACE_ROOT: /sandbox/runs
      AF_SANDBOX_COMPAT_INPUT_PATH: "true"
    volumes:
      - ./data/agent_datasets:/data/agent_datasets
      - ./data/database_fetched:/data/database_fetched
      - ./data/manifests:/data/manifests
      - ./data/agent_artifacts:/data/agent_artifacts
      - /var/run/docker.sock:/var/run/docker.sock
    expose:
      - "8095"
    deploy:
      resources:
        limits:
          memory: 1G
    networks:
      - alphafrog-network

  python-sandbox-gateway-service:
    image: alphafrog-micro-python-sandbox-gateway-service:latest
    build: ./pythonSandboxGatewayService
    container_name: alphafrog-python-sandbox-gateway-service
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      nacos:
        condition: service_healthy
      python-sandbox-service:
        condition: service_started
    environment:
      TZ: Asia/Shanghai
      NACOS_ADDRESS: nacos
      AF_SANDBOX_SERVICE_URL: http://python-sandbox-service:8095
      # Memory Optimization (Light)
      JAVA_TOOL_OPTIONS: "-Xms256m -Xmx256m"
    ports:
      - "50060:50060"
    deploy:
      resources:
        limits:
          memory: 512M
    networks:
      - alphafrog-network

  frontend:
    image: alphafrog-micro-frontend:latest
    build: ./frontend
    container_name: alphafrog-frontend
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      nacos:
        condition: service_healthy
      domestic-stock-service:
        condition: service_started
      domestic-index-service:
        condition: service_started
      domestic-fund-service:
        condition: service_started
      domestic-listed-asset-service:
        condition: service_started
      admin-service:
        condition: service_started
      portfolio-service:
        condition: service_started
      agent-langchain-service:
        condition: service_started
      external-info-service:
        condition: service_started
    environment:
      TZ: Asia/Shanghai
      AF_DB_MAIN_HOST: ${AF_DB_MAIN_HOST:-host.docker.internal}
      AF_DB_MAIN_PORT: 5432
      AF_DB_MAIN_DATABASE: ${AF_DB_MAIN_DATABASE:-alphafrog}
      AF_DB_MAIN_USER: ${AF_DB_MAIN_USER:-alphafrog}
      AF_DB_MAIN_PASSWORD: ${AF_DB_MAIN_PASSWORD:-alphafrog_pass}
      AF_RABBITMQ_HOST: rabbitmq
      AF_RABBITMQ_PORT: 5672
      AF_RABBITMQ_USER: ${AF_RABBITMQ_USER:-alphafrog}
      AF_RABBITMQ_PASS: ${AF_RABBITMQ_PASS:-Excited1s}
      AF_REDIS_HOST: redis
      AF_REDIS_PORT: 6379
      AF_REDIS_PASSWORD: ${AF_REDIS_PASSWORD:-Excited1s}
      NACOS_ADDRESS: nacos
      # 可选：覆盖 JWT 密钥（默认值在 frontend 的 application.yml 里）
      # AF_JWT_SECRET: ${AF_JWT_SECRET}
      AF_RAG_INGEST_TOKEN: ${AF_RAG_INGEST_TOKEN:-}
      AF_CONFIG_NACOS_ENABLED: "true"
      AF_CONFIG_NACOS_NAMESPACE: ${AF_CONFIG_NACOS_NAMESPACE:-}
      AGENT_LANGCHAIN_PROVIDER_DUBBO_GROUP: ${AGENT_LANGCHAIN_PROVIDER_DUBBO_GROUP:-langchain}
      # Memory Optimization (Medium)
      JAVA_TOOL_OPTIONS: "-Xms384m -Xmx512m"
    ports:
      - "8090:8090"
    deploy:
      resources:
        limits:
          memory: 768M
    networks:
      - alphafrog-network

volumes:
  alphafrog_pgdata:
  alphafrog_redisdata:
  alphafrog_rabbitmq:

networks:
  alphafrog-network:
    driver: bridge
    # 勿在已运行的 stack 上改 driver_opts：compose 会尝试删网重建，其它容器仍挂网时会失败。
    # 若需 enable_icc，须 docker compose down 后全栈 up（见 deploy 文档 / thread e13717e0）。
    ipam:
      config:
        - subnet: 172.18.0.0/16
          gateway: 172.18.0.1
  shared-infra:
    name: shared-infra
    external: true            # 不创建，使用已存在的网络（由 ~/infra/docker-compose.yml 管理）
