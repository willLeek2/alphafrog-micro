<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="world.willfrog.agent.platform.mapper.SchedulerStateMapper">

    <!--
      调度器全局状态：全局新增暂停状态与跨图轮转轮次。两张表都是「作用域做主键、一行一条事实」，
      读到的值就是权威，写入不带条件。
    -->

    <resultMap id="SchedulerCapacityStateResultMap"
               type="world.willfrog.agent.platform.capacity.SchedulerCapacityState">
        <id property="scopeKey" column="scope_key"/>
        <result property="unfinishedCount" column="unfinished_count"/>
        <result property="addPaused" column="add_paused"/>
        <result property="pausedSince" column="paused_since"/>
        <result property="highWatermark" column="high_watermark"/>
        <result property="lowWatermark" column="low_watermark"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="loadCapacityState" resultMap="SchedulerCapacityStateResultMap">
        SELECT scope_key,
               unfinished_count,
               add_paused,
               paused_since,
               high_watermark,
               low_watermark,
               updated_at
        FROM alphafrog_agent_scheduler_capacity_state
        WHERE scope_key = #{scopeKey}
    </select>

    <!--
      写入一次判定结果：数量、暂停标记、暂停起始时间一起落库。暂停起始时间只在「从没暂停变成暂停」
      那一刻写入，一直暂停期间保持不变；恢复时清空。
    -->
    <select id="applyCapacityDecision" resultMap="SchedulerCapacityStateResultMap"
            affectData="true" flushCache="true">
        UPDATE alphafrog_agent_scheduler_capacity_state SET
            unfinished_count = #{unfinishedCount},
            add_paused = #{paused}::boolean,
            paused_since = CASE
                WHEN #{paused}::boolean THEN COALESCE(paused_since, CURRENT_TIMESTAMP)
                ELSE NULL
            END,
            high_watermark = #{highWatermark},
            low_watermark = #{lowWatermark},
            updated_at = CURRENT_TIMESTAMP
        WHERE scope_key = #{scopeKey}
        RETURNING scope_key,
                  unfinished_count,
                  add_paused,
                  paused_since,
                  high_watermark,
                  low_watermark,
                  updated_at
    </select>

    <select id="advanceRound" resultType="java.lang.Long" affectData="true" flushCache="true">
        UPDATE alphafrog_agent_scheduler_round SET
            round_number = round_number + 1,
            updated_at = CURRENT_TIMESTAMP
        WHERE scope_key = #{scopeKey}
        RETURNING round_number
    </select>

    <select id="currentRound" resultType="java.lang.Long">
        SELECT round_number
        FROM alphafrog_agent_scheduler_round
        WHERE scope_key = #{scopeKey}
    </select>

</mapper>
